export interface TopIcons {
    iconName: string;
    title: string;
    color: string
}

export interface BottomDetails {
    iconName: string;
    title: string;
    color: string;
    text: string
}